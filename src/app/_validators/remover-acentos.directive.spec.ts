import { RemoverAcentosDirective } from './remover-acentos.directive';

describe('RemoverAcentosDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoverAcentosDirective();
    expect(directive).toBeTruthy();
  });
});
